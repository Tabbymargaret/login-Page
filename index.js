window.innerWidth = 900;
window.innerHeight = 600;

const winHeight = window.innerHeight;
const winWidth = window.innerWidth;
const aspectRatio = winWidth / winHeight;


console.log(winHeight, winWidth, aspectRatio);
